import { Host1Directive } from './host1.directive';

describe('Host1Directive', () => {
  it('should create an instance', () => {
    const directive = new Host1Directive();
    expect(directive).toBeTruthy();
  });
});
